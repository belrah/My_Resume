const fullName = (name, surname_name) => {
    return `My name is ${name} ${surname_name}`;
};

document.write(fullName('Mercy', 'Kalu')); //This should be able to write Mercy Kalu on the page

//copy and paste the above code to check on the console